# 시스템 관제 자동화 스크립트 개발 — 수행 내역서 & 단계별 실행 가이드

> **이 문서 하나로 과제의 모든 단계를 수행할 수 있습니다.**  
> 명령어는 `코드블록` 안에 있으며, **그대로 복사-붙여넣기** 하면 됩니다.  
> 각 단계마다 "✅ 확인 방법"이 있으니 꼭 확인 후 다음으로 넘어가세요.

---

## 📌 사전 준비 — 아키텍처 확인

터미널을 열고 아래 명령어를 입력합니다:

```bash
uname -m
```

- `x86_64` 출력 → **agent-app-linux-x86** 파일 사용
- `aarch64` 출력 → **agent-app-linux-arm64** 파일 사용

> 이 가이드는 **x86_64** 기준으로 작성되었습니다. arm64라면 파일명만 바꾸세요.

---

## STEP 1 — 기본 보안 설정 (SSH + 방화벽)

### 1-1. SSH 포트 변경 및 Root 로그인 차단

```bash
sudo nano /etc/ssh/sshd_config
```

파일이 열리면 아래 두 줄을 찾아 수정합니다  
(앞에 `#`이 있으면 지워서 활성화):

```
Port 20022
PermitRootLogin no
```

저장: `Ctrl+O` → `Enter` → `Ctrl+X`

SSH 서비스 재시작:

```bash
sudo systemctl restart sshd
```

**✅ 확인:**
```bash
sudo ss -tulnp | grep sshd
```
→ `:20022` 가 출력되면 성공

```bash
grep -E "^Port|^PermitRootLogin" /etc/ssh/sshd_config
```
→ `Port 20022`과 `PermitRootLogin no` 가 출력되면 성공

---

### 1-2. 방화벽(UFW) 설정

> ⚠️ **중요**: 방화벽 활성화 전에 반드시 SSH 포트(20022)를 먼저 허용해야 합니다.  
> 순서를 지키지 않으면 서버에서 잠길 수 있습니다.

```bash
sudo ufw allow 20022/tcp
sudo ufw allow 15034/tcp
sudo ufw enable
```

`Command may disrupt existing ssh connections. Proceed with operation (y|n)?` 라고 물으면 `y` 입력.

**✅ 확인:**
```bash
sudo ufw status
```
→ 아래처럼 출력되면 성공:
```
Status: active
20022/tcp   ALLOW   Anywhere
15034/tcp   ALLOW   Anywhere
```

---

## STEP 2 — 계정/그룹 생성

### 2-1. 그룹 먼저 생성

```bash
sudo groupadd agent-common
sudo groupadd agent-core
```

### 2-2. 계정 생성

```bash
sudo useradd -m -s /bin/bash agent-admin
sudo useradd -m -s /bin/bash agent-dev
sudo useradd -m -s /bin/bash agent-test
```

### 2-3. 그룹에 계정 추가

| 계정 | agent-common | agent-core |
|------|:---:|:---:|
| agent-admin | ✅ | ✅ |
| agent-dev | ✅ | ✅ |
| agent-test | ✅ | ❌ |

```bash
# agent-common 그룹에 세 계정 모두 추가
sudo usermod -aG agent-common agent-admin
sudo usermod -aG agent-common agent-dev
sudo usermod -aG agent-common agent-test

# agent-core 그룹에 admin, dev만 추가
sudo usermod -aG agent-core agent-admin
sudo usermod -aG agent-core agent-dev
```

**✅ 확인:**
```bash
id agent-admin
id agent-dev
id agent-test
```
→ `agent-admin`과 `agent-dev`는 두 그룹 모두, `agent-test`는 `agent-common`만 포함

---

## STEP 3 — 디렉토리 구조 및 권한 설정

### 3-1. 환경 변수 설정 (먼저 결정)

AGENT_HOME 경로를 `/home/agent-admin/agent-app`으로 설정합니다.

```bash
sudo -u agent-admin bash -c 'echo "export AGENT_HOME=/home/agent-admin/agent-app
export AGENT_PORT=15034
export AGENT_UPLOAD_DIR=\$AGENT_HOME/upload_files
export AGENT_KEY_PATH=\$AGENT_HOME/api_keys/t_secret.key
export AGENT_LOG_DIR=/var/log/agent-app" >> /home/agent-admin/.bashrc'
```

### 3-2. 디렉토리 생성

```bash
sudo mkdir -p /home/agent-admin/agent-app/upload_files
sudo mkdir -p /home/agent-admin/agent-app/api_keys
sudo mkdir -p /home/agent-admin/agent-app/bin
sudo mkdir -p /var/log/agent-app
```

### 3-3. 키 파일 생성

```bash
echo "agent_api_key_test" | sudo tee /home/agent-admin/agent-app/api_keys/t_secret.key
```

### 3-4. 소유자/그룹 설정

```bash
# agent-app 전체: agent-admin 소유
sudo chown -R agent-admin:agent-admin /home/agent-admin/agent-app

# upload_files: agent-common 그룹, R/W 가능
sudo chown agent-admin:agent-common /home/agent-admin/agent-app/upload_files
sudo chmod 770 /home/agent-admin/agent-app/upload_files

# api_keys: agent-core 그룹 ONLY
sudo chown agent-admin:agent-core /home/agent-admin/agent-app/api_keys
sudo chmod 750 /home/agent-admin/agent-app/api_keys

# api_keys 내 파일도 동일하게
sudo chown agent-admin:agent-core /home/agent-admin/agent-app/api_keys/t_secret.key
sudo chmod 640 /home/agent-admin/agent-app/api_keys/t_secret.key

# /var/log/agent-app: agent-core 그룹 ONLY
sudo chown agent-admin:agent-core /var/log/agent-app
sudo chmod 770 /var/log/agent-app
```

### 3-5. ACL 설정 (세밀한 권한 제어)

`acl` 패키지가 없으면 먼저 설치:

```bash
sudo apt-get install -y acl
```

```bash
# upload_files: agent-common 그룹 rwx
sudo setfacl -m g:agent-common:rwx /home/agent-admin/agent-app/upload_files

# api_keys: agent-core 그룹만 rwx, others 접근 차단
sudo setfacl -m g:agent-core:rwx /home/agent-admin/agent-app/api_keys
sudo setfacl -m o::--- /home/agent-admin/agent-app/api_keys

# /var/log/agent-app: agent-core 그룹만 rwx
sudo setfacl -m g:agent-core:rwx /var/log/agent-app
```

**✅ 확인:**
```bash
ls -la /home/agent-admin/agent-app/
getfacl /home/agent-admin/agent-app/upload_files
getfacl /home/agent-admin/agent-app/api_keys
getfacl /var/log/agent-app
```

---

## STEP 4 — 앱 파일 배치 및 실행

### 4-1. 앱 파일 복사

앱 파일이 있는 위치에서 아래 명령어를 실행합니다  
(파일을 직접 다운로드하거나 복사한 경로로 수정하세요):

```bash
# 예: 파일이 /tmp/agent-app-linux-x86 에 있는 경우
sudo cp /tmp/agent-app-linux-x86 /home/agent-admin/agent-app/agent-app-linux-x86
sudo chmod +x /home/agent-admin/agent-app/agent-app-linux-x86
sudo chown agent-admin:agent-admin /home/agent-admin/agent-app/agent-app-linux-x86
```

### 4-2. 앱 실행 (agent-admin 계정으로)

```bash
sudo -u agent-admin bash -c '
export AGENT_HOME=/home/agent-admin/agent-app
export AGENT_PORT=15034
export AGENT_UPLOAD_DIR=$AGENT_HOME/upload_files
export AGENT_KEY_PATH=$AGENT_HOME/api_keys/t_secret.key
export AGENT_LOG_DIR=/var/log/agent-app
$AGENT_HOME/agent-app-linux-x86
'
```

**✅ 성공 기준:**
```
Starting Agent Boot Sequence...
[1/5] Checking User Account               [OK]
[2/5] Verifying Environment Variables     [OK]
[3/5] Checking Required Files             [OK]
[4/5] Checking Port Availability          [OK]
[5/5] Verifying Log Permission            [OK]
------------------------------------------------------------
All Boot Checks Passed!
Agent READY
```

**✅ 포트 확인 (다른 터미널에서):**
```bash
ss -tulnp | grep 15034
```
→ `0.0.0.0:15034` LISTEN 상태 확인

> 앱 종료: `Ctrl+C`

---

## STEP 5 — monitor.sh 배치 및 권한 설정

### 5-1. monitor.sh 파일 복사

제공된 `monitor.sh` 파일을 서버로 옮깁니다:

```bash
sudo cp /tmp/monitor.sh /home/agent-admin/agent-app/bin/monitor.sh
```

> 로컬에서 서버로 파일을 옮길 때는 scp 사용:  
> `scp -P 20022 monitor.sh 사용자명@서버IP:/tmp/`

### 5-2. 소유자/그룹/권한 설정

```bash
# 소유자: agent-dev / 그룹: agent-core / 권한: 750
sudo chown agent-dev:agent-core /home/agent-admin/agent-app/bin/monitor.sh
sudo chmod 750 /home/agent-admin/agent-app/bin/monitor.sh
```

**✅ 확인:**
```bash
ls -la /home/agent-admin/agent-app/bin/monitor.sh
```
→ `-rwxr-x--- 1 agent-dev agent-core` 가 출력되면 성공

### 5-3. monitor.sh 수동 실행 테스트

(앱이 실행 중인 상태에서)

```bash
sudo -u agent-admin bash -c '
export AGENT_HOME=/home/agent-admin/agent-app
/home/agent-admin/agent-app/bin/monitor.sh
'
```

**✅ 확인:**
- `[HEALTH CHECK]` 섹션에 `[OK]` 출력
- `[RESOURCE MONITORING]` 섹션에 CPU/MEM/DISK 수치 출력
- `/var/log/agent-app/monitor.log` 에 로그 기록 확인:

```bash
tail -5 /var/log/agent-app/monitor.log
```

---

## STEP 6 — crontab 등록 (매분 자동 실행)

### 6-1. agent-admin의 crontab 편집

```bash
sudo crontab -u agent-admin -e
```

처음 실행 시 편집기 선택을 물으면 `1` (nano) 입력.

파일 맨 아래에 추가:

```
* * * * * AGENT_HOME=/home/agent-admin/agent-app AGENT_PORT=15034 AGENT_UPLOAD_DIR=/home/agent-admin/agent-app/upload_files AGENT_KEY_PATH=/home/agent-admin/agent-app/api_keys/t_secret.key AGENT_LOG_DIR=/var/log/agent-app /home/agent-admin/agent-app/bin/monitor.sh >> /var/log/agent-app/monitor-cron.log 2>&1
```

저장: `Ctrl+O` → `Enter` → `Ctrl+X`

### 6-2. 등록 확인

```bash
sudo crontab -u agent-admin -l
```

### 6-3. 1~2분 후 로그 자동 누적 확인

```bash
# 실시간으로 로그 증가 감시
tail -f /var/log/agent-app/monitor.log
```

→ 1분마다 새 줄이 추가되면 성공. 종료: `Ctrl+C`

---

## STEP 7 — 최종 증거 자료 수집 (수행 내역서용)

아래 명령어들을 실행하고 결과를 캡처하거나 복사해 두세요.

```bash
echo "=== 1. SSH 포트 확인 ==="
grep -E "^Port|^PermitRootLogin" /etc/ssh/sshd_config
sudo ss -tulnp | grep sshd

echo "=== 2. 방화벽 상태 ==="
sudo ufw status

echo "=== 3. 계정/그룹 확인 ==="
id agent-admin
id agent-dev
id agent-test

echo "=== 4. 디렉토리 권한 확인 ==="
ls -la /home/agent-admin/agent-app/
getfacl /home/agent-admin/agent-app/upload_files
getfacl /home/agent-admin/agent-app/api_keys
getfacl /var/log/agent-app

echo "=== 5. monitor.sh 파일 권한 ==="
ls -la /home/agent-admin/agent-app/bin/monitor.sh

echo "=== 6. monitor.log 최근 기록 ==="
tail -10 /var/log/agent-app/monitor.log

echo "=== 7. crontab 등록 확인 ==="
sudo crontab -u agent-admin -l
```

---

## 📋 필수 증거 자료 체크리스트

| 항목 | 확인 명령어 | 성공 기준 |
|------|------------|---------|
| SSH 포트(20022) 변경 | `ss -tulnp \| grep sshd` | `:20022` 출력 |
| Root 원격 접속 차단 | `grep PermitRootLogin /etc/ssh/sshd_config` | `PermitRootLogin no` |
| UFW 활성화 | `ufw status` | `Status: active` |
| 20022/15034 포트만 허용 | `ufw status` | 두 포트만 ALLOW |
| 계정 생성 | `id agent-admin` 등 | 각 계정 존재 확인 |
| 그룹 멤버십 | `id agent-admin` | agent-core, agent-common 포함 |
| 디렉토리 권한 | `getfacl` 결과 | upload:common, api_keys:core |
| 앱 Boot Sequence | 앱 실행 출력 | 5단계 [OK] + Agent READY |
| monitor.sh 실행 | monitor.sh 출력 | HEALTH/RESOURCE 섹션 정상 |
| monitor.log 누적 | `tail monitor.log` | 타임스탬프 라인 누적 |
| crontab 매분 등록 | `crontab -u agent-admin -l` | `* * * * *` 라인 존재 |
| 1분 후 로그 증가 | `tail -f monitor.log` | 1분마다 새 라인 추가 |

---

## 💡 과제 목표 — 개념 이해 정리

### ① SSH 포트 변경과 Root 원격 접속 차단이 기본 보안인 이유

**SSH 포트 변경 (22 → 20022)**  
전 세계 해커들의 자동화 스캔 봇(bot)은 22번 포트에 집중적으로 무작위 로그인 시도(Brute Force)를 합니다. 포트를 바꾸면 이런 자동화 공격의 대부분을 차단할 수 있습니다. "문을 잠그는" 것이 아니라 "문을 찾기 어렵게" 만드는 방어입니다.

**Root 원격 로그인 차단**  
Root는 서버의 모든 권한을 가진 최고 관리자 계정입니다. Root 로그인이 허용되어 있으면, 비밀번호 하나만 뚫리면 서버 전체가 장악됩니다. 일반 계정으로 접속 후 `sudo`를 통해 필요한 작업만 수행하는 것이 안전합니다 → **최소 권한 원칙(Principle of Least Privilege)**.

---

### ② UFW로 "필요 포트만 허용"하는 방화벽 정책

방화벽은 외부에서 들어오는 네트워크 트래픽을 필터링하는 문지기입니다.

```
외부 인터넷
     │
  [UFW 방화벽]
  ├─ :20022 (SSH)  → ✅ 허용
  ├─ :15034 (APP)  → ✅ 허용
  └─ 나머지 모든 포트 → ❌ 차단
     │
  [서버 내부]
```

`ufw enable`을 하면 기본적으로 모든 인바운드 연결을 차단하고, 명시적으로 허용한 포트만 열립니다 (기본 거부 정책, Default Deny). 이렇게 하면 포트를 이용한 외부 침입 경로를 최소화할 수 있습니다.

---

### ③ 역할 기반 계정/그룹 + ACL로 디렉토리를 분리하는 이유

**역할 기반 계정 분리**
- `agent-admin`: 서비스 실행, cron 관리
- `agent-dev`: 스크립트 개발, 코드 수정
- `agent-test`: 기능 테스트만 수행

각 역할마다 필요한 권한만 부여하면, 실수나 침해가 발생해도 피해 범위를 제한할 수 있습니다.

**공유 디렉토리 vs 보안 디렉토리**

| 디렉토리 | 그룹 | 이유 |
|----------|------|------|
| `upload_files` | agent-common (admin+dev+test 모두) | 팀 전체가 파일을 공유해야 함 |
| `api_keys` | agent-core (admin+dev만) | 비밀 키는 테스터가 볼 필요 없음 |
| `/var/log/agent-app` | agent-core (admin+dev만) | 로그에 민감 정보가 포함될 수 있음 |

**ACL(Access Control List)** 은 리눅스의 기본 권한(소유자/그룹/기타)보다 더 세밀하게 권한을 지정할 수 있는 방법입니다. 예를 들어, "A 그룹에는 읽기+쓰기, B 그룹에는 읽기만"처럼 그룹별로 다른 권한을 부여할 수 있습니다.

---

### ④ 환경 변수(AGENT_HOME 등)로 실행 환경을 고정하는 이유

코드 안에 경로를 직접 쓰면:
```
/home/agent-admin/agent-app/api_keys/t_secret.key  # 하드코딩 ❌
```

나중에 경로가 바뀌면 코드 전체를 수정해야 합니다.

환경 변수를 쓰면:
```bash
$AGENT_KEY_PATH  # 환경 변수 참조 ✅
```

경로가 바뀌어도 환경 변수 한 줄만 수정하면 됩니다. 또한 같은 코드를 개발 환경(`/home/dev/...`)과 운영 환경(`/opt/prod/...`)에서 다르게 실행할 수 있습니다.

**검증 방법:**
```bash
echo $AGENT_HOME      # 경로 출력 확인
echo $AGENT_PORT      # 포트 출력 확인
printenv | grep AGENT # 모든 AGENT 관련 환경변수 출력
```

---

### ⑤ 쉘 스크립트로 프로세스/포트/리소스 수집 → 로그로 추적하는 흐름

```
[monitor.sh 실행]
       │
       ▼
프로세스 살아있나? (pgrep)
       ├─ 없음 → exit 1 (알람)
       └─ 있음 → PID 기록
       │
       ▼
포트 열려있나? (ss -tulnp)
       ├─ 닫힘 → exit 1 (알람)
       └─ 열림 → OK
       │
       ▼
CPU/MEM/DISK 수집 (top, free, df)
       │
       ▼
임계값 초과? → [WARNING] 출력
       │
       ▼
monitor.log에 한 줄 기록
[YYYY-MM-DD HH:MM:SS] PID:... CPU:..% MEM:..% DISK:..%
```

로그가 쌓이면 "언제 CPU가 치솟았는가", "장애 직전 메모리 상태"를 분석할 수 있습니다. **데이터 없이는 원인 분석이 불가능합니다.**

---

### ⑥ crontab으로 주기 실행 + 로그 보존 정책이 필요한 이유

**crontab 시간 표현식:**
```
* * * * * 명령어
│ │ │ │ │
│ │ │ │ └── 요일 (0=일, 6=토)
│ │ │ └──── 월 (1-12)
│ │ └────── 일 (1-31)
│ └──────── 시 (0-23)
└────────── 분 (0-59)
```

`* * * * *` = 매분 실행

**로그 보존 정책이 필요한 이유:**
- 로그는 계속 쌓이면 디스크를 가득 채웁니다
- 디스크가 꽉 차면 서버 전체가 다운될 수 있습니다
- 해결책: 10MB 초과 시 파일을 새로 만들고, 10개 이상이면 가장 오래된 것 삭제

```
monitor.log         (현재, 계속 쓰는 중)
monitor.log.20260101_120000   (이전, 보관)
monitor.log.20260101_110000   (더 이전, 보관)
... 최대 10개
```

---

## 📁 제출 파일 목록

1. **수행내역서_및_단계별가이드.md** (이 파일)
2. **monitor.sh** (자동화 스크립트 소스코드)
